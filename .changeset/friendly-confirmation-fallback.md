---
"@trainheroic-unofficial/core": patch
---

Use the explicit `confirm:true` recovery flow when a modern MCP client cannot show confirmation
prompts, returning model-readable guidance while keeping the confirmation gate closed.
