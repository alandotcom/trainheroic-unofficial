---
"@trainheroic-unofficial/cloudflare": patch
---

Update dependencies. Worker runtime deps move: `agents` 0.16.2 → 0.17.1, `hono` 4.12.26 → 4.12.27, `@sentry/cloudflare` 10.59.0 → 10.62.0. Dev tooling also refreshed (`oxlint`, `oxfmt`, `wrangler`, `@cloudflare/vitest-pool-workers`, `@types/node`, and `@hono/node-server` v1 → v2 in the eval harness).
