---
import Layout from "../layouts/Layout.astro";
import Header from "../components/Header.astro";
import Footer from "../components/Footer.astro";
import { relativeHref } from "../lib/paths.ts";
---

<Layout
  title="Developers"
  description="Claude Code skill, TypeScript SDK, and MCP reference for TrainHeroic integrations."
>
  <Header />
  <main id="main">
    <div class="shell narrow">
      <header class="doc__intro">
        <h1>Developers</h1>
        <p class="lead">
          Documentation for Claude Code, scripts, and custom apps. To connect Claude.ai in the
          browser, use the <a href={relativeHref("/", Astro.url.pathname)}>connector setup</a> on the home page.
        </p>
      </header>

      <section class="doc">
        <ul class="doc-list">
          <li>
            <a href={relativeHref("skill", Astro.url.pathname)}>Claude Code skill</a>: run <code>npx skills add</code>, then the agent
            calls <code>trainheroic</code>.
          </li>
          <li>
            <a href={relativeHref("sdk", Astro.url.pathname)}>TypeScript SDK</a>: package <code>@trainheroic-unofficial/js</code> for
            scripts and workers.
          </li>
          <li>
            <a href={relativeHref("mcp", Astro.url.pathname)}>MCP reference</a>: Cursor, Claude Desktop, local stdio servers, and the
            tool catalog.
          </li>
        </ul>
      </section>
    </div>
  </main>
  <Footer />
</Layout>
