---
import { sitePath, withBase } from "../lib/paths.ts";

interface Props {
  title: string;
  description: string;
  path?: string;
}

const { title, description, path = sitePath(Astro.url.pathname) } = Astro.props;
const canonical = new URL(path === "/" ? "." : path.slice(1), `${Astro.site}/`);
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta
      name="viewport"
      content="width=device-width, initial-scale=1, viewport-fit=cover"
    />
    <meta name="description" content={description} />
    <link rel="canonical" href={canonical} />
    <link rel="icon" type="image/svg+xml" href={withBase("favicon.svg")} />
    <title>{title} · trainheroic-unofficial</title>
  </head>
  <body>
    <a class="skip-link" href="#main">Skip to main content</a>
    <slot />
  </body>
</html>

<script>
  import "../scripts/copy.ts";
</script>

<style is:global>
  @import "../styles/global.css";
</style>
