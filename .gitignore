# Node / pnpm / build
node_modules/
dist/
*.log
# MCPB build output (the bundled server). The manifest and icon are committed.
packages/coach-mcp/mcpb/server/

# Cloudflare / wrangler
.wrangler/
.dev.vars
.dev.vars.*
!.dev.vars.example
**/worker-configuration.d.ts

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# OS
.DS_Store

# Local TrainHeroic state (lives in ~/.trainheroic, never in-repo)
.trainheroic/
session.json
*.db
*.sqlite
*.sqlite3

# Editor / env
.env
*.local
.claude/worktrees/

# mitmproxy capture dumps (large, contain live session tokens) — never commit
flows
flows[0-9]*
